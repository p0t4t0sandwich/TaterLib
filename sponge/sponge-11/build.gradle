import org.spongepowered.gradle.plugin.config.PluginLoaders
import org.spongepowered.plugin.metadata.model.PluginDependency

plugins {
    id("org.spongepowered.gradle.plugin") version('2.1.1')
}

java.disableAutoTargetJvm()

sponge {
    apiVersion(spongeapi_version)
    license(project.license)
    loader {
        name(PluginLoaders.JAVA_PLAIN)
        version("1.0")
    }
    plugin(project_id) {
        version("${minecraft_version}-${project.version}")
        displayName(project_name)
        entrypoint("${group_id}.${project_id}.loader.platforms.Sponge8LoaderPlugin")
        description(project_description)
        links {
            homepage(homepage_url)
            source(repo_url)
            issues(issue_tracker_url)
        }
        dependency("spongeapi") {
            loadOrder(PluginDependency.LoadOrder.AFTER)
            optional(false)
        }
        dependency("luckperms") {
            version("5.3.0")
            loadOrder(PluginDependency.LoadOrder.AFTER)
            optional(true)
        }
    }
}

dependencies {
    compileOnly('com.mojang:brigadier:1.0.18')
    compileOnly('org.spongepowered:sponge:1.20.2-11.0.0-SNAPSHOT')
    compileOnly(project(':common'))
}

jar {
    destinationDirectory = file('../../target')
    archiveFileName = "${project_id}-sponge-11-${version}.jar"
}
