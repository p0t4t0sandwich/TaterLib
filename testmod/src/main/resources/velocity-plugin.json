{
  "id": "${mod_id}",
  "name": "${mod_name}",
  "version": "${version}",
  "description": "${description}",
  "url": "${homepage_url}",
  "authors": [
    "${author}"
  ],
  "dependencies": [
    {
      "id": "taterlib",
      "optional": false
    }
  ],
  "main": "${group}.platforms.VelocityPlugin"
}
