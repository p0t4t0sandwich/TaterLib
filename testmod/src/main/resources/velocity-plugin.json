{
  "id": "${project_id}",
  "name": "${project_name}",
  "version": "${version}",
  "description": "${project_description}",
  "url": "${homepage_url}",
  "authors": [
    "${authors}"
  ],
  "dependencies": [
    {
      "id": "taterlib",
      "optional": false
    }
  ],
  "main": "${group}.platforms.VelocityPlugin"
}
