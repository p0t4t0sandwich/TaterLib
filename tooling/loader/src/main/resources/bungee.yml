name: '${project_name}'
version: '${version}'
author: '${authors}'
description: '${project_description}'
website: '${homepage_url}'
main: '${group_id}.${project_id}.loader.platforms.BungeeLoaderPlugin'
depends: [ ]
softdepends: [ LuckPerms ]
commands:
  taterlib:
    description: TaterLib command
    usage: /taterlib <reload|version>
    permission: taterlib.command
