{
  "loader": {
    "name": "java_plain",
    "version": "1.0"
  },
  "license": "${license}",
  "plugins": [
    {
      "id": "${mod_id}",
      "entrypoint": "dev.neuralnexus.taterloader.platforms.Sponge8LoaderPlugin",
      "name": "${mod_name}",
      "description": "${description}",
      "version": "${version}",
      "branding": {},
      "links": {
        "homepage": "${homepage_url}",
        "source": "${source_url}",
        "issues": "${issue_url}"
      },
      "dependencies": [
        {
          "id": "spongeapi",
          "version": "${spongeapi_version}",
          "load-order": "after",
          "optional": false
        },
        {
          "id": "luckperms",
          "version": "${luckperms_version}",
          "load-order": "after",
          "optional": true
        },
        {
          "id": "permissionsex",
          "version": "${permissionsex_version}",
          "load-order": "after",
          "optional": true
        }
      ]
    },
    {
      "id": "muxins",
      "name": "Muxins",
      "version": "${modlib_version}"
    }
  ]
}
