{
  "id": "${mod_id}",
  "name": "${mod_name}",
  "version": "${version}",
  "description": "${description}",
  "url": "${homepage_url}",
  "authors": [
    "${authors}"
  ],
  "dependencies": [
    {
      "id":"luckperms",
      "optional": true
    },
    {
      "id":"permissionsex",
      "optional": true
    }
  ],
  "main": "dev.neuralnexus.taterloader.platforms.VelocityLoaderPlugin"
}
