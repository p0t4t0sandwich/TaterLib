plugins {
    id('com.github.johnrengelman.shadow') version('7.1.2')
}

dependencies {
    // YAML
    implementation('dev.dejvokep:boosted-yaml:1.3.1')

    // MySQL
    implementation("com.zaxxer:HikariCP:4.0.3")
    implementation("mysql:mysql-connector-java:8.0.26")

    // MongoDB
    implementation('org.mongodb:mongodb-driver-sync:4.9.1')
    implementation('org.mongodb:mongodb-driver-core:4.9.1')
    implementation('org.mongodb:bson:4.9.1')

    // LuckPerms
    compileOnly("net.luckperms:api:5.4")

    // Logging
    compileOnly('org.slf4j:slf4j-api:2.0.5')
}

shadowJar {
    dependencies {
        // YAML
        include(dependency("dev.dejvokep:boosted-yaml:1.3.1"))

        // MySQL
        include(dependency("com.zaxxer:HikariCP:4.0.3"))
        include(dependency("mysql:mysql-connector-java:8.0.26"))

        // MongoDB
        include(dependency('org.mongodb:mongodb-driver-sync:4.9.1'))
        include(dependency('org.mongodb:mongodb-driver-core:4.9.1'))
        include(dependency('org.mongodb:bson:4.9.1'))
    }
    // YAML
    relocate('dev.dejvokep', 'dev.neuralnexus.taterapi.lib.dejvokep')

    // MySQL
    relocate('com.zaxxer.hikari', 'dev.neuralnexus.taterapi.lib.hikari')
    relocate('com.mysql', 'dev.neuralnexus.taterapi.lib.mysql')

    // MongoDB
    relocate('com.mongodb', 'dev.neuralnexus.taterapi.lib.mongodb')
    relocate('org.bson', 'dev.neuralnexus.taterapi.lib.bson')

    destinationDir(file('../target'))
    archiveName("TaterAPI-${version}-common.jar")
    classifier('')
}
shadowJar.doLast {
    task ->
        ant.checksum(file: task.archivePath) {
            hashAlgorithm = 'md5'
            property = 'md5sum'
        }
}

artifacts {
    archives(shadowJar)
}
