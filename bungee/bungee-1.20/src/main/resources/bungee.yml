name: '${project_name}'
version: '${version}'
author: '${authors}'
description: '${project_description}'
main: '${group_id}.${project_id}.bungee.Bungee${project_name}Plugin'
depends: [ ]
softdepends: [ LuckPerms ]
commands:
  taterlib:
    description: TaterLib command
    usage: /taterlib <reload|version>
    permission: taterlib.command
